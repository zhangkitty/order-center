

const defaultState = {

};

const reducer = (state = defaultState, action) =>
  // switch (action.type) {
  //   case TYPES.INIT:
  //     return defaultState;
  //
  //   default:
  //     return state;
  // }
  defaultState;
export default reducer;

